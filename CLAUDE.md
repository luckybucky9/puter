# Claude Code Instructions

Follow `AGENTS.md`.

Claude Code sessions must use Puter or the documented manual fallback before editing. If a claim returns `409 Conflict`, stop implementation and route through the returned conflict/integration issue.
