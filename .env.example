PUTER_PORT=8787
PUTER_HOST=127.0.0.1
PUTER_CONFIG=./puter.config.json
PUTER_API_TOKEN=replace-with-local-token

LINEAR_API_KEY=replace-with-linear-api-key
GITHUB_TOKEN=replace-with-github-token

# Optional. Used by the Symphony integration client when a project defines a refresh URL.
SYMPHONY_REFRESH_TOKEN=
