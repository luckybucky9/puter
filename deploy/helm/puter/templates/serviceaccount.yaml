{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "puter.serviceAccountName" . }}
  labels:
    {{- include "puter.labels" . | nindent 4 }}
{{- end }}
